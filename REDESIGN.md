# خطة إعادة التصميم — من تطبيق يعمل إلى تطبيق يُشتهى

هذه خطة تنفيذية، لا مزاج. مبنية على مرجعين بصريين اخترتَهما:

- **المرجع الأخضر** (رمضان الداكن): أخضر عميق + ذهبي، بطاقات كبيرة الاستدارة، قوس عدّاد
  للصلاة القادمة، شريط دوائر علوي، بطاقة عادة بمؤشّر تقدّم، زر ذهبي عريض.
- **المرجع الكريمي** (المصحف الفاتح): خلفية عاجية دافئة، بطاقة «آخر قراءة» ذهبية متدرّجة،
  فلاتر حبّة (سورة/جزء/صفحة)، بلوك آية بلا حدود، بوصلة دائرية ناعمة، شريط سفلي دافئ.

القاسم المشترك بينهما ليس اللون — بل **الذهب كلهجة، والاستدارة الكبيرة، والمساحة البيضاء
بدل الخطوط الفاصلة**. وهذا بالضبط ما سنبني عليه، مع الحفاظ على الأخضر كهوية التطبيق حتى لا
يستيقظ المستخدم على تطبيق لا يعرفه.

---

## 0) القرار الحاكم: لغة واحدة، مزاجان

| | الوضع الفاتح «نور» | الوضع الداكن «ليل» |
|---|---|---|
| الأرضية | عاجي دافئ `#FBF6EF` مع وشاح متدرّج خفيف | أخضر–أسود `#0B1512` |
| السطح | أبيض `#FFFFFF` عند 92% | أخضر داكن `#12211C` |
| السطح المرتفع | `#FFFDF9` + ظل ناعم | `#18302A` |
| الحبر | أخضر عميق `#0E2B22` | عاجي `#F3EFE7` |
| الهوية | أخضر `#0F6B4F` | أخضر `#10B981` |
| اللهجة | ذهبي `#E0A32E` | ذهبي `#F0B429` |
| الزجاج | أبيض 55% + ضباب 18 | أسود 38% + ضباب 22 |

**الأخضر يبقى هوية، والذهب يبقى لهجة.** الذهب لا يُستخدم إلا في: العناصر الحيّة (الوقت
القادم)، بطاقة الأبطال (Hero)، والزر الأساسي الواحد في الشاشة. أكثر من ذلك يتحوّل الذهب من
لهجة إلى ضجيج — وهو ما يفسد المرجعين لو قُلِّدا حرفياً.

**تحذير تباين:** الذهبي على العاجي لا يصلح للنص أبداً (نسبة تباين ~2:1). النص فوق أي سطح
ذهبي يكون **أخضر عميق `#0E2B22`**، لا أبيض. هذه ليست تفصيلة تجميلية — بدونها لا يقرأ نصف
المستخدمين البطاقة في الشمس.

---

## 1) الأساس: `design_tokens.dart`

ملف واحد يصير مصدر الحقيقة، ويُمنع بعده أي `Color(0x...)` أو `EdgeInsets` سحري داخل الصفحات.

```
lib/core/theme/design_tokens.dart
├── AppColors      ground / surface / surfaceRaised / ink / inkMuted / brand / gold / glass
├── AppRadii       xs 10 · sm 14 · md 20 · lg 28 · xl 36 · pill 999
├── AppSpacing     xs 4 · sm 8 · md 12 · lg 16 · xl 24 · xxl 32
├── AppShadows     soft (y8 b24 a.06) · lift (y16 b36 a.10) · glow(color)
└── AppGradients   heroGold · heroGreen · meshLight · meshDark · ringGlow
```

الاستدارة الكبيرة هي أوضح ما في المرجعين: **البطاقات 28، الحقول والحبّات 999، الأيقونات
المربّعة 14**. لا شيء بين 14 و28 — التدرّج نصف المُتقن هو ما يجعل الواجهة تبدو «مصنوعة على
عجل».

**الظل بدل الحد.** كل `Border.all` في التطبيق الحالي يُستبدل بظل ناعم أو باختلاف سطح. هذا
امتداد طبيعي لقاعدتك: لا حدود جانبية.

---

## 2) مكتبة المكوّنات — 12 قطعة تُبنى مرة وتُستعمل في كل مكان

| المكوّن | مأخوذ من | يستبدل اليوم |
|---|---|---|
| `AppScaffold` | الوشاح المتدرّج في المرجعين | كل `Scaffold` خام |
| `MeshBackground` | الخلفية العاجية المتوهّجة | خلفية مسطّحة |
| `SectionHeader` | «Al Quran + الفلاتر» | عناوين يدوية متكرّرة |
| `PillSelector` | حبّات Surah/Juz/Page | `SegmentedButton` الافتراضي |
| `HeroCard` | «Last Read» الذهبية | بطاقات الرئيسية الأربع |
| `AppListRow` | صف السورة (رقم + اسم + وصف + عربي) | 6 تنويعات صفوف مختلفة |
| `ArcGauge` | قوس 03:45 الأخضر | العدّاد النصّي |
| `ProgressCard` | «Do You Fast Today? 06/30» | بطاقة الورد والختمة |
| `StoryRail` | دوائر المساجد العلوية | لا يوجد |
| `AyahBlock` | بلوك الآية بلا حدود | عرض الآية الحالي |
| `GhostIconButton` | صف (تشغيل/حفظ/مشاركة) | أزرار أيقونات متفرّقة |
| `HintPill` | «Rotate the phone 135°» | نص إرشادي عادي |

القيمة هنا ليست جمالية فقط: اليوم صفّ السورة وصفّ الجزء وصفّ الذِّكر وصفّ التنزيل **أربع
قطع كود مختلفة تفعل الشيء نفسه**. توحيدها في `AppListRow` يقلّص الكود ويجعل أي تعديل لاحق
تعديلاً واحداً.

---

## 3) الشاشات — ماذا يتغيّر بالضبط

### الرئيسية
- رأس: تحية + **اسم المكان** (بعد إصلاح الموقع) + جرس الإشعارات — كالمرجع الأخضر.
- **`ArcGauge`**: قوس نصف دائري للصلاة القادمة، طرفاه الفجر والمغرب، العقرب ذهبي، والزمن
  المتبقّي في المنتصف بخط العرض. هذا أوضح بمراحل من سطر نصّي.
- شريط `StoryRail` تحته: آخر قراءة · وردك · القبلة · التسبيح — دوائر بحلقة متدرّجة.
- بطاقات: `HeroCard` لآخر قراءة (ذهبية بصورة مئذنة مرسومة)، ثم `ProgressCard` للورد،
  ثم بطاقة الختمة، ثم اللافتة الموسمية.

### فهرس المصحف
- `SectionHeader` مع **`PillSelector`**: سورة · جزء · حزب · صفحة · سجدة — بدل الأزرار
  الحالية. الحبّة المختارة سطح أبيض بظل ناعم، لا لون صارخ (هذا سرّ نظافة المرجع الكريمي).
- `AppListRow`: رقم في شارة، الاسم اللاتيني/العربي، سطر صغير «مكية · ٧ آيات»، والاسم العربي
  بخط المصحف على الطرف الآخر.
- حقل بحث زجاجي عائم يبقى ثابتاً أعلى القائمة.

### القارئ
- رأس: اسم السورة بخط العرض، سطر «الفاتحة · ٧ آيات»، وبسملة داخل إطار زخرفي مرسوم.
- **`AyahBlock`**: شارة `١:١` حبّة رمادية باهتة، الآية بخط المصحف، الترجمة/التفسير تحتها
  بلون باهت، وصف أيقونات شبحية. **الفاصل بين الآيات مسافة، لا خط.**
- شريط أدوات سفلي زجاجي يظهر عند التمرير لأعلى ويختفي عند النزول.

### مواقيت الصلاة
- `ArcGauge` كبير في الأعلى + اسم المدينة تحته.
- صفوف الصلوات: أيقونة دائرية (شمس/غروب/هلال)، الاسم، النطاق الزمني، ومفتاح صوت على الطرف
  — نسخة مباشرة من المرجع الأخضر لأنه أفضل مما لدينا.
- الصلاة الحالية بخلفية ذهبية خفيفة وحدّ داخلي مضيء.

### القبلة (طلبك الصريح)
- دائرة كبيرة ناعمة على خلفية الوشاح، حلقة خارجية رفيعة، أربع نقاط للاتجاهات الفرعية،
  وحروف N/S/E/W بخط خفيف.
- الإبرة من المركز بمحور دائري، **مطفية رمادية بعيداً عن القبلة وتتحوّل ذهبية ثم خضراء عند
  الضبط** (الاهتزاز المتدرّج موجود بالفعل).
- علامة الكعبة على المحيط + سهم مؤشّر أعلى الدائرة.
- تحتها: الدرجة برقم كبير، ثم **`HintPill`**: «أدر الهاتف ١٣٥° لليسار» — الجملة الإرشادية
  هي ما يجعل البوصلة قابلة للاستخدام فعلاً.
- اسم المكان أعلى الشاشة بدل الإحداثيات.

### الأذكار والتسبيح
- بطاقات الفئات: شبكة 2×N ببطاقات مستديرة، أيقونة مرسومة، وعدّاد «٣٠ ذكراً».
- شاشة الذكر: `AyahBlock` نفسه للآيات (بخط المصحف تلقائياً)، وبطاقة بيضاء للدعاء، وعدّاد
  دائري كبير أسفل الشاشة.
- التسبيح: دائرة ضخمة قابلة للنقر في المنتصف، رقم بخط العرض، وحلقة تقدّم ذهبية.

### الإعدادات والثانوية
- صفوف موحّدة بأيقونة دائرية + عنوان + وصف + سهم/مفتاح — الشكل الموجود اليوم صحيح، يحتاج
  فقط رموز التصميم الجديدة.

---

## 4) الشريط السفلي والزجاج

الشريط الحالي (الرئيسية في دائرة مرفوعة في المنتصف) **يبقى كما هو بنيةً** ويُعاد تلوينه:

- الحاوية: زجاج بضباب 18 وحدّ علوي أبيض 10%، استدارة 28، وظل رفع.
- الدائرة الوسطى: تدرّج أخضر→ذهبي رفيع الحواف، حلقة بلون الأرضية 4px، وتوهّج تحتها.
- العناصر الجانبية: أيقونة فقط + نقطة صغيرة متحرّكة تحت المختار (لا نصوص — كما في مرجعك).
- **شرط الزجاج:** كل صفحة تستعمل `AppScaffold` تحصل تلقائياً على `extendBody` و110px حشو
  سفلي. الزجاج بلا محتوى يمرّ تحته = مستطيل رمادي بلا معنى.

---

## 5) المواسم فوق النظام الجديد

الثيم الموسمي لا يعيد تعريف الألوان — يبدّل **الذهب والوشاح فقط**:

| الموسم | اللهجة | الوشاح |
|---|---|---|
| رمضان | ذهبي `#F0B429` | كحلي → أخضر داكن |
| العشر الأواخر | ذهبي فاتح `#FFD166` | أغمق + نجوم |
| عيد الفطر | فيروزي `#2DD4BF` | فيروزي → عاجي |
| عيد الأضحى | بنّي دافئ `#C9884A`* | رملي → بنّي |

\* القيمة النهائية تُضبط من `seasonal_theme.dart` القائم.

وصفحة **معاينة المواسم** (نُفِّذت الآن) تسمح بارتداء أي موسم في أي يوم وتجربة مقدمته —
وهي الطريقة الوحيدة العملية لاختبار شيء لا يظهر إلا أسبوعين في السنة.

---

## ✅ حالة التنفيذ

**نُفِّذت الدفعات الثماني كلها (D0–D7).** ما تحقّق فعلياً:

- `design_tokens.dart` مصدر وحيد للّون والمسافة والاستدارة والظل والحركة، و`AppTheme` يُبنى منه.
- ١٢ مكوّناً مشتركاً + **معرض مكوّنات داخلي** يبدّل المزاج والموسم حيّاً (الإعدادات ← معرض المكوّنات).
- كل الشاشات أُعيد بناؤها: الرئيسية (قوس + شريط دوائر + بطاقة ذهبية)، المصحف (فلاتر حبّة + صفوف موحّدة)،
  القارئ (رأس مقوّس مرسوم)، الصلاة، **صفحة قبلة مستقلة**، الأذكار والتسبيح، والبقية على `AppScaffold`.
- المواسم أُعيد بناؤها **داخل** النظام: كل موسم يبدّل اللهجة والوشاح والتدرّج فقط، والهوية الخضراء ثابتة.
- **اختبارات تحرس النظام**: تباين كل لون، عدم وجود لون خارج ملف الرموز، وثبات الهوية عبر المواسم.

---

## 6) خطة التنفيذ — سبع دفعات، كل واحدة تُشحن وحدها

| # | الدفعة | المخرجات | الأثر |
|---|---|---|---|
| D0 | رموز التصميم | `design_tokens.dart` + إعادة بناء `AppTheme` من الرموز | لا شيء يتكسّر، كل شيء يصير قابلاً للتغيير من مكان واحد |
| D1 | المكتبة | 12 مكوّناً + قصص اختبار في صفحة `?/design` داخلية | أساس كل ما بعده |
| D2 | الرئيسية | `ArcGauge` + `StoryRail` + `HeroCard` | أول انطباع، أعلى عائد |
| D3 | المصحف | الفهرس + `PillSelector` + `AyahBlock` | أكثر الشاشات استعمالاً |
| D4 | الصلاة والقبلة | صفوف الصلاة + البوصلة الجديدة + `HintPill` | طلبك الصريح |
| D5 | الأذكار والتسبيح | بطاقات الفئات + العدّاد الدائري | أكثر شاشة تحتاج تنظيماً |
| D6 | البقية | الإعدادات، الحفظ، الإحصاءات، التنزيلات | إغلاق الفجوة البصرية |
| D7 | الصقل | حركة، اهتزاز، حالات فارغة، تباين، RTL | الفرق بين «جميل» و«مصقول» |

**ترتيب مقصود:** D0 وD1 لا يُريان في التطبيق لكن بدونهما تتحوّل كل دفعة تالية إلى قصّ ولصق.
وأي محاولة للبدء من D2 مباشرة ستنتهي بستّ نسخ من البطاقة نفسها.

---

## 7) ما لن نأخذه من المرجعين، وليه

- **الخلاصة الاجتماعية** (صور المستخدمين والإعجابات) — تحتاج خادماً وحسابات وإشرافاً على
  المحتوى. تطبيقنا يعمل دون إنترنت بالكامل، وهذه ميزة لا نضحّي بها من أجل شكل.
- **البثّ المباشر من المساجد** — حقوق بثّ وتكلفة تشغيل، ليست قراراً تصميمياً.
- **الأخضر الليموني الصارخ في خلفية المرجع** — يبدو رائعاً في صورة عرض، ويؤذي العين في
  تطبيق يُفتح خمس مرات يومياً قبل الفجر.
- **النص الأبيض فوق الذهبي** — يسقط في اختبار التباين كما شرحنا أعلاه.

---

## 8) كيف نعرف أننا نجحنا

1. صفر `Color(0x` خارج `design_tokens.dart`.
2. كل شاشة تستعمل `AppScaffold` ولا شيء يختفي تحت الشريط الزجاجي.
3. لقطة من كل شاشة في الوضعين × الأربعة مواسم — من صفحة المعاينة، لا بانتظار رمضان.
4. `flutter analyze` نظيف و`flutter test` أخضر بعد كل دفعة، لا في النهاية.
