# مقدمات المناسبات (Seasonal intros)

الملفات هنا تُشغَّل مرة واحدة يومياً عند فتح التطبيق في موسمها، وتُتخطّى بلمسة، ويمكن
إطفاؤها من الإعدادات. القاعدة الوحيدة غير القابلة للتفاوض: **المقدمة لا تؤخّر التطبيق أبداً**
— إن فشل التحميل أو تأخّر، يمضي التطبيق فوراً.

| الملف | المناسبة | المستخدم |
|---|---|---|
| `ramadan_intro.mp4` | رمضان والعشر الأواخر | `SeasonalIntroService.assets` |
| `eid_adha_intro.mp4` | عيد الأضحى | `SeasonalIntroService.assets` |
| `eid_fitr_intro.mp4` | عيد الفطر | غير مضاف بعد |

## المواصفات المطلوبة للملف

| البند | المطلوب | الملف الحالي |
|---|---|---|
| الأبعاد | **1080×1920** (رأسي 9:16) | 1080×1920 ✅ |
| المدة | **3.5–4.5 ثانية** | 4.00 ثوانٍ ✅ |
| الحجم | أقل من 2 ميجابايت | ~0.82 ميجابايت ✅ |
| الترميز | H.264 / yuv420p / 30fps | ✅ |
| الصوت | AAC، ذروة −3dB، مع تلاشٍ في آخر 0.5 ثانية | AAC 128kbps، ذروة −2.9dB ✅ |
| الإطار الأخير | لون خلفية التطبيق الصلب `#111827` | ✅ |

المشهد الأصلي (هلال، فوانيس، **مدفع الإفطار**، خط رمضان كريم) بقي كما هو؛
أُعيد تأطيره رأسياً وخُصرت مدته من 10 ثوانٍ إلى 4، ثم يتلاشى إلى `#111827` حتى
يقطع التطبيق إلى الشاشة الرئيسية بلا ومضة.

## ألوان التطبيق (استخدمها كما هي)

| الدور | HEX |
|---|---|
| الخلفية الداكنة | `#111827` |
| أزرق رمضان الأعمق | `#10233F` |
| أزرق رمضان الأفتح | `#1D3B5C` |
| الذهبي (الفوانيس والهلال والزخارف) | `#F0B429` |
| الذهبي الفاتح (توهّج ونصّ) | `#FCE8B2` |
| الأخضر الأساسي (لمسة واحدة فقط) | `#10B981` |

## كيف يُعاد إنتاج الملف (بدون إعادة تصميم)

لا تُرسل المشهد إلى مولّد فيديو ليبتكر شكلاً جديداً — النتيجة تفقد المدفع
والتفاصيل. المطلوب إعادة **ماستر** من المصدر الأفقي:

1. المصدر: المشهد ثلاثي الأبعاد الأصلي 1280×720 / 10 ثوانٍ (الهلال، الفوانيس، المدفع، النص).
2. أعد التأطير إلى 1080×1920 مع بقاء كامل المشهد في الثلث العلوي–الأوسط؛ الثلث السفلي هادئ.
3. اسرّع إلى 4 ثوانٍ مع الحفاظ على طبقة الصوت الأصلية (من دون تغيير الطبقة).
4. ذروة الصوت −3dBFS، تلاشٍ صامت في آخر 0.5 ثانية، AAC 128kbps داخل MP4 + WAV منفصل.
5. الإطار الأخير 0.3 ثانية لون صلب `#111827` بلا عناصر.
6. لا قصّ يبتلع المدفع أو الفوانيس، لا فلاش، لا حركة كاميرا سريعة، لا إطار نهائي ساطع.

## كيف تُعرض المقدمة

الفيديو **لا يملأ الشاشة**. يظهر داخل **إطار** بنفس شكل بطاقات التطبيق وبحلقة ذهبية رفيعة، وبقيّة
الشاشة تأخذ خلفية الثيم الحالي بوشاحه الموسمي، وفوقه تحية الموسم بخط العناوين وتحته شريط تقدّم.
السبب: مقطع أفقي على شاشة رأسية يُقصّ قصّاً شديداً، وملء الشاشة يصنع حدّاً حادّاً بين «فيديو»
و«تطبيق» — الإطار يجعله جزءاً من المنتج نفسه.

## إن لم تعمل المقدمة

أول ما تتحقّق منه هو **حجم الملف**: نسخة منقوصة تفتح ولا تُفكّ شفرتها، فتظهر شاشة ساكنة ثم يُكمل
التطبيق. `test/bundled_assets_test.dart` يفحص الحجم ووجود صندوق `mdat` كاملاً، فيمسك هذا الخطأ
قبل الإطلاق. والمهلة الآن ٦ ثوانٍ لفتح الملف بدل ٣، لأن جهازاً بطيئاً يقضي معظمها في الفتح.

## بعد استلام الملف الجديد

```bash
cp ~/Downloads/<new-file>.mp4 assets/video/ramadan_intro.mp4
flutter run   # افتح التطبيق في رمضان أو بدّل تاريخ الجهاز للتجربة
```

لا حاجة لأي تعديل في الكود؛ الاسم والمسار ثابتان في `SeasonalIntroService.assets`.
مقدمة العيد تُضاف لاحقاً بنفس المواصفات باسم `eid_intro.mp4` وسطر واحد في نفس الخريطة.
