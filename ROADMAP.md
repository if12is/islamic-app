# خارطة تطوير المصحف والتنبيهات — Islamic App

> 65 فكرة مرتّبة حسب الاعتمادية، مبنية على قراءة الكود الفعلي في `lib/` بتاريخ 19 أغسطس 2026.
> النسخة التفاعلية: <https://claude.ai/code/artifact/ef9639a8-64dc-4be3-857b-36d391ff5377>

## حالة التنفيذ (آخر تحديث: 24 أغسطس 2026)

**كل ما يمكن تنفيذه بلا جهاز حقيقي ولا خادم صار منفَّذاً.** الباقي خمسة بنود، كلٌّ منها
موقوف على سبب مذكور بالاسم في الجدول أسفل. الرموز:
`✅` منفَّذ · `🟡` منفَّذ جزئياً · `⬜` لم يبدأ.

| ما تغيّر فعلياً | أين |
|---|---|
| المصحف كامل داخل التطبيق (نص + صفحات + أجزاء + سجدات) | `lib/features/quran/data/services/quran_local_service.dart` |
| القارئ لا يلمس الشبكة إطلاقاً، والبحث صار محلياً ويتجاهل التشكيل | `surah_reader_page.dart`, `quran_page.dart` |
| لوحة تحكم القراءة: خط/حجم/تباعد/هوامش/أوضاع عين/سطوع/تمرير تلقائي | `reader_settings_provider.dart`, `reader_settings_sheet.dart` |
| مرجعيات متعددة بوسوم وملاحظات + صفحة مرجعيات | `bookmark_store.dart`, `bookmarks_page.dart` |
| أدوات الآية: نسخ/مشاركة نص/مشاركة صورة/استماع/تفسير/ملاحظة | `ayah_actions_sheet.dart`, `ayah_share_card.dart` |
| التفسير لكل آية (5 نسخ) مع كاش دائم | `tafsir_service.dart`, `tafsir_sheet.dart` |
| تلاوة بالآية مع تظليل متزامن وتشغيل في الخلفية | `quran_audio_provider.dart` + `just_audio_background` |
| مواقيت محسوبة على الجهاز (تعمل بلا إنترنت ولأي تاريخ) | `prayer_calculation_service.dart`, `prayer_times_calculator_datasource.dart` |
| جدولة إشعارات حقيقية 7 أيام + إعادة الجدولة بعد التشغيل/تغيّر الموقع أو الطريقة | `notification_service.dart`, `notification_scheduler.dart` |
| مركز تنبيهات كامل بمعاينة «القادم» وزر اختبار | `notification_center_page.dart` |
| خطوط مرفقة: ReemKufi للعناوين، AmiriQuran و ScheherazadeNew للمصحف | `assets/fonts/`, `app_theme.dart` |
| فهرس متقدّم: سور/أجزاء/**أحزاب**/**صفحات**/**سجدات** + «انتقل إلى» بمرجع دقيق | `quran_page.dart`, `quran_meta_data.dart` |
| صفحة «تدبّراتي» ببحث وترتيب وتحرير وتصدير | `notes_page.dart` |
| مشغّل كامل: سرعة، تكرار آية، تكرار مقطع بعدد مرات، مؤقت نوم | `player_sheet.dart`, `quran_audio_provider.dart` |
| ضبط المواقيت: مذهب العصر، خطوط العرض، إزاحة لكل صلاة، تعديل هجري، بمعاينة حيّة | `prayer_settings_page.dart`, `prayer_settings_store.dart` |
| تقويم هجري كامل بالمناسبات وأيام الصيام ومواقيت كل يوم | `hijri_calendar_page.dart`, `hijri_service.dart` |
| أزرار في الإشعارات (افتح/استمع) وفتح الشاشة الصحيحة من الإشعار | `notification_router.dart` |
| اختيار صوت الأذان بقناة مستقلة لكل صوت | `notification_service.dart` (`adhanSounds`) |
| 111 اختبار وحدة تغطي الحساب والجدولة والمصحف والتقويم والختمة وبيانات الأذكار | `test/` |
| حصن المسلم كاملاً: 136 باباً و352 ذكراً بفضلها وتخريجها، مرفقة بلا شبكة | `assets/data/azkar.json` |
| خطة ختمة + ورد يومي + سلسلة أيام وخريطة سنة، من سجل قراءة يُملأ تلقائياً | `reading_progress_store.dart`, `khatmah_plan.dart`, `reading_stats_page.dart` |
| تنبيهات الجمعة والصيام والمناسبات الهجرية | `notification_scheduler.dart`, `hijri_service.dart` |
| ويدجت الشاشة الرئيسية للمواقيت | `widget_service.dart`, `PrayerWidgetProvider.kt` |
| ثلاثة أذانات مرفقة + استيراد أي ملف صوتي + أذان خاص بالفجر | `res/raw/`, `adhan_sound_service.dart` |
| **تمييز الآيات داخل الأذكار بخط المصحف تلقائياً** — مطابقة بنوافذ أربع كلمات على فهرس المصحف كله، تتحمّل فروق الرسم العثماني (`النفثت` مقابل `النفاثات`) | `quran_text_detector.dart`, `arabic_text_block.dart` |
| إمساكية بعمود اليوم ومشاركتها **كصورة بوستر عربي** بقوس وفوانيس مرسومة بالكود | `imsakiya_poster.dart`, `islamic_ornaments.dart` |
| نظام خطوط موحّد: ReemKufi للعناوين · Cairo للنص · AmiriQuran للآيات | `app_text_styles.dart` |
| شريط تنقّل زجاجي عائم وحقول بحث زجاجية (فاتح وداكن) | `glass_container.dart`, `glass_nav_bar.dart` |
| إمساكية رمضان لكل أيام الشهر داخل التقويم + تصديرها | `imsakiya_table.dart` |
| وضع صفحات المصحف (604) بجانب الوضع المتصل | `surah_reader_page.dart` |
| منظومة حفظ كاملة: إخفاء تدريجي، مراجعة متباعدة، واختبار «أكمل الآية» | `hifz_*.dart` |
| نسخ احتياطي واستعادة بملف JSON يملكه المستخدم | `backup_service.dart` |
| وضع رمضان: عدّاد الإمساك/الإفطار وتراويح والعشر الأواخر | `ramadan_card.dart` |
| مكتبة قرّاء وتنزيل السور للاستماع دون إنترنت | `audio_download_service.dart`, `downloads_page.dart` |
| **ثيم موسمي لرمضان والعيدين**: ألوان ولافتة مرسومة (فوانيس، هلال، خروف) تظهر وتختفي وحدها | `seasonal_theme.dart`, `seasonal_banner.dart` |
| **بوصلة قبلة جديدة**: زجاجية، إبرة مُنعَّمة، اهتزاز يتسارع كلما اقتربت، ولون يتحول للأخضر عند الضبط | `qibla_compass.dart`, `qibla_math.dart` |
| **الموقع بالاسم** مع اختيار مدينة يدوياً أو العودة للتلقائي | `location_service.dart`, `location_picker_sheet.dart` |
| تتبّع آخر قراءة تلقائياً: يُحفظ عند التمرير واللمس ومغادرة التطبيق | `surah_reader_page.dart`, `last_read_card.dart` |
| تبديل القارئ أثناء التشغيل يكمل من نفس الموضع بدل أن يتوقف | `quran_audio_provider.dart`, `quran_page.dart` |
| **شريط تنقّل بالرئيسية في المنتصف داخل دائرة مرفوعة** وخمسة أقسام بأيقونات فقط | `glass_nav_bar.dart` |
| **مقدمة موسمية عند فتح التطبيق في رمضان** مرة يومياً، تُتخطّى بلمسة ولها مفتاح في الإعدادات | `seasonal_intro_service.dart`, `seasonal_intro_screen.dart` |
| **نظام تصميم كامل**: رموز في ملف واحد، ١٢ مكوّناً مشتركاً، معرض داخلي، واختبارات تباين | `design_tokens.dart`, `core/widgets/*`, `design_gallery_page.dart` |
| **إعادة تصميم كل الشاشات** على النظام الجديد: الرئيسية والمصحف والصلاة والقبلة والأذكار والباقي | `REDESIGN.md` |
| **اسم المكان بلا إنترنت**: ٣٢٠٠ مدينة بمحافظتها ودولتها داخل التطبيق | `nearest_city_service.dart`, `assets/data/cities.json` |
| **التسبيح المطلق**: عدّاد بلا نهاية محفوظ للأبد، يُكتب مع كل ضغطة ولا يُصفَّر إلا بتأكيد | `tasbeeh_store.dart` |
| **دخول موسمي**: مآذن وقباب وفوانيس متأرجحة في شاشة البداية وبطاقة رمضان الذهبية | `seasonal_art.dart` |
| **معاينة المواسم**: ارتداء أي موسم في أي يوم لتجربة ألوانه ومقدمته | `seasonal_preview_page.dart` |
| **التسميع الصوتي (تجريبي)**: يستمع لتلاوتك ويلوّن كل كلمة أخضر/أصفر/أحمر | `recitation_service.dart`, `recitation_match.dart`, `recitation_page.dart` |
| **أسماء الله الحسنى** و**الصلاة على النبي ﷺ** و**حاسبة الزكاة** و**سجل الصلوات** | `divine_names.dart`, `salawat_*.dart`, `zakat_*.dart`, `prayer_log_*.dart` |
| **تصدير الآية كفيديو**: استوديو بمعاينة حيّة، والقطع عند نهاية كل آية بقياس زمنها لا بتقدير | `ayah_video_spec.dart`, `ayah_video_frame.dart`, `ayah_video_exporter_ffmpeg.dart` |
| **مشغّل كامل بملء الشاشة** وجلسة تشغيل واحدة يشترك فيها الشريط الصغير والمشغّل | `now_playing_page.dart`, `surah_audio_provider.dart` |
| **قوائم استماع** بورد صوتي يومي، و**تقرير أسبوعي** قابل للمشاركة كصورة | `playlist_store.dart`, `weekly_report.dart` |
| **مكتبة أدعية بالمواقف** و**الرقية بتشغيل متواصل** مع تمييز الوارد مما يُقرأ لموضوعه | `dua_library.dart`, `ruqyah_passages.dart` |
| **إمكانية وصول** باسم لكل زر أيقونة، و**تخزين وتوفير بيانات** بأرقام حقيقية | `accessibility_test.dart`, `storage_page.dart`, `data_saver.dart` |
| **تذكير الورد في وقتك المعتاد**، محسوباً على الجهاز من عدّادات ساعات فقط | `wird_habit_store.dart` |

**ما لم يُنفَّذ بعد ولماذا (آخر مراجعة: 24 أغسطس 2026):**

| البند | لماذا لم يُنفَّذ |
|---|---|
| **H08** ختمة جماعية | مؤجَّلة بطلبك؛ تحتاج خادماً وحسابات، وهي قرار منتج قبل أن تكون قرار كود. |
| **R07** الترجمة والكلمة‑بكلمة | بيانات ضخمة (٦٢٣٦ آية × عدة ترجمات) وتصميم عرض مستقل. شغل كبير قائم بذاته. |
| **N05** إسكات الهاتف وقت الصلاة | يحتاج إذن `ACCESS_NOTIFICATION_POLICY` واستعادة مضمونة للحالة — يُختبر على جهاز حقيقي، وترك الهاتف صامتاً لأن التطبيق أُغلق خطأ لا يُغتفر. |
| **N08** إشعار ثابت للصلاة القادمة | يحتاج جهازاً للتحقق من سلوك الإشعار الدائم على هونور وون بلس. |
| **S08** روابط عميقة | تحتاج إعداد App Links والتحقق من النطاق على جهاز. |
| **P05** القبلة بالكاميرا | 🟡 البوصلة نفسها مكتملة؛ تبقى طبقة الكاميرا وتحتاج جهازاً. |
| **Z03** سبحة متطوّرة · **S07** ترتيب الأذونات | 🟡 مغطّاة عملياً بالتسبيح الحالي واختيار المدينة يدوياً. |

## أين كان التطبيق قبل التنفيذ (الفجوات الأصلية)

| الحالة | الفجوة | الموضع |
|---|---|---|
| 🔴 لا تعمل | `NotificationService` فيه `show()` فوري فقط — لا `zonedSchedule` ولا مهمة خلفية. الأذان لا يصل إن لم يُفتح التطبيق. حزمة `timezone` مضافة وغير مستخدمة. | `lib/core/services/notification_service.dart` |
| 🔴 هشّ | القارئ ينادي `fetchSurah()` من الشبكة عند كل فتح بلا كاش؛ بلا إنترنت لا قراءة. `QuranCacheService` يخزّن الفهرس فقط ولا يستخدمه القارئ. | `surah_reader_page.dart`, `quran_cache_service.dart` |
| 🟠 محدود | تحكّم القراءة = حجم الخط فقط، ومرجعية واحدة تُستبدل مع كل لمسة آية. | `surah_reader_page.dart` |
| 🟠 ناقص | `azkar.json` = 3.4KB، ثلاث فئات (4/3/4 عناصر). حصن المسلم 130+ باب. | `assets/data/azkar.json` |
| 🟠 محدود | الصوت: 3 قرّاء، سورة كاملة فقط، بلا خلفية/تكرار/سرعة/تنزيل. | `quran_page.dart:88-100` |
| 🟢 جاهز | Clean Architecture + Riverpod + `Either<Failure,T>` + `SecureHttpClient` + Material 3 — أساس يستحمل كل ما يلي. | `lib/core/`, `lib/features/prayer_times/` |

## 1) محرك القراءة (15)

| # | الحالة | الفكرة | التنفيذ |
|---|---|---|---|
| R01 | ✅ | القرآن كاملاً داخل التطبيق (نص + صفحات + أجزاء + سجدات) | **منفَّذ:** حزمة `quran` عبر `QuranLocalService`؛ الشبكة للتفسير والصوت فقط |
| R02 | ✅ | وضع صفحات المصحف (604 صفحة) | **منفَّذ:** `PageView` بترتيب المصحف مع تبديل «متصل/صفحات» من لوحة القراءة (بالخطوط المرفقة لا QCF) |
| R03 | ✅ | لوحة تحكم القراءة (خط/حجم/تباعد/هوامش) | **منفَّذ:** `readerSettingsProvider` + ورقة إعدادات بمعاينة حيّة |
| R04 | ✅ | أوضاع العين: تلقائي/نهاري/سيبيا/ليلي/أخضر + قفل السطوع + إبقاء الشاشة | **منفَّذ:** `ReaderPalette` + `wakelock_plus` + `screen_brightness` |
| R05 | ✅ | تمرير تلقائي بسرعة قابلة للضبط | **منفَّذ:** `Ticker` على `ScrollController` مع حفظ السرعة |
| R06 | ✅ | التفسير عند كل آية (الميسّر/الجلالين/الوسيط/البغوي/القرطبي) | **منفَّذ:** نسخ alquran.cloud تُجلب للسورة كاملة وتُخزَّن في Hive للأبد |
| R07 | ⬜ | الترجمة والكلمة‑بكلمة | ترجمات حزمة `quran` المرفقة، أو `words` من quran.com v4 |
| R08 | ✅ | تلوين التجويد مع مفتاح ألوان | **منفَّذ:** اثنتا عشرة قاعدة تُشتقّ من الرسم العثماني نفسه — لا أصل بيانات خارجي — مع مفتاح ألوان، والتلوين يفسح للتظليل أثناء التلاوة |
| R09 | ✅ | قائمة أدوات الآية (نسخ/مشاركة/استماع/تفسير/مرجعية/ملاحظة) | **منفَّذ:** لمسة = تحديد، ولمسة ثانية تفتح الأدوات؛ الحفظ إجراء صريح |
| R10 | ✅ | مرجعيات متعددة بوسوم وألوان وملاحظات | **منفَّذ:** صندوق Hive `quran_bookmarks` + صفحة مرجعيات بفلاتر وحذف بالسحب |
| R11 | ✅ | مشاركة الآية كبطاقة صورة | **منفَّذ:** `screenshot` + `share_plus` بثلاثة قوالب (زمردي/ليلي/ورقي) |
| R12 | ✅ | بحث offline يتجاهل التشكيل والهمزات | **منفَّذ:** `QuranLocalService.search` على النص المجرَّد؛ FTS5 لاحقاً لو احتجنا سرعة أعلى |
| R13 | ✅ | فهرس متقدّم (سور/أجزاء/أحزاب/أرباع/صفحات/سجدات) | **منفَّذ:** خمسة أوضاع فهرسة + «انتقل إلى» بمرجع دقيق، وبيانات الأحزاب الـ240 مولّدة داخل الكود |
| R14 | ✅ | الملاحظات والتدبّر | **منفَّذ:** صفحة «تدبّراتي» ببحث وترتيب (الأحدث/ترتيب المصحف) وتحرير وتصدير نصي |
| R15 | ✅ | استئناف من آخر آية قرأتها لا من أول السورة | **منفَّذ:** الموضع يُحفظ عند التمرير وعند لمس آية وعند مغادرة التطبيق، وبطاقة «آخر قراءة» تقرأ الحالة الحيّة لا لقطة قديمة |

## 2) الصوت والاستماع (7)

| # | الحالة | الفكرة | التنفيذ |
|---|---|---|---|
| A01 | ✅ | تشغيل بالآية مع تظليل متزامن وتمرير تابع | **منفَّذ:** `setAudioSources` بملفات cdn.islamic.network + مزامنة `currentIndexStream` |
| A02 | ✅ | تشغيل مستمر في الخلفية مع إشعار تحكم | **منفَّذ:** `just_audio_background` + `AudioServiceActivity` وخدمة في الـmanifest |
| A03 | ✅ | تكرار المقطع للحفظ (من آية إلى آية × N) | **منفَّذ:** اختيار النطاق وعدد التكرارات مع عدّاد تقدّم في المشغّل |
| A04 | ✅ | سرعة التلاوة ومؤقت النوم | **منفَّذ:** سرعة 0.5–2× ومؤقت 15/30/45/60 دقيقة أو «نهاية المقطع» |
| A05 | ✅ | مكتبة قرّاء موسّعة مع تنزيل للاستماع دون إنترنت | **منفَّذ:** 7 قرّاء، تنزيل بالسورة بشريط تقدّم وإلغاء، وتشغيل يفضّل الملف المحلي، مع إدارة المساحة |
| A06 | ✅ | قوائم استماع وورد صوتي | **منفَّذ:** قوائم بالسور في Hive مع سحب لإعادة الترتيب، وتشغيل متتابع بطابور في `SurahAudioController`، وقائمة واحدة تُعلَّم «وردي الصوتي اليومي» يُسجَّل إتمامها |
| A08 | ✅ | مشغّل كامل بملء الشاشة | **منفَّذ:** غلاف مرسوم، شريط تمرير قابل للسحب، ±١٠ ثوانٍ، السابق/التالي، سرعة، تكرار، مؤقت نوم، واختيار القارئ بالرواية — والشريط الصغير أصبح ملخّصاً لنفس الجلسة لا مشغّلاً ثانياً |
| A09 | ✅ | تصدير الآية كفيديو | **منفَّذ:** استوديو بمعاينة حيّة (نطاق الآيات، الخط، الحجم، اللوحة، الزخرفة، المقاس، القارئ) يقيس زمن كل آية بـ`ffprobe` فيقطع عندها بالضبط، ويرمّز MP4 بـ`ffmpeg_kit_flutter_new_min_gpl` |
| A07 | ✅ | تسميع بالصوت (تجريبي) | **منفَّذ:** `speech_to_text` بمحرّك الجهاز نفسه (بلا تسجيل ولا رفع ولا نموذج يُنزَّل)، ومحاذاة كلمة بكلمة على الهيكل المجرَّد تتحمّل حذف كلمة أو إقحامها، فتلوّن الصحيح أخضر والقريب أصفر والمشكوك فيه أحمر مع نسبة دقة. الشاشة تقول صراحةً إنها عون على المراجعة لا حكم |

## 3) الحفظ والختمة والإحصاءات (8)

| # | الحالة | الفكرة | التنفيذ |
|---|---|---|---|
| H01 | ✅ | خطة ختمة تعيد توزيع نفسها | **منفَّذ:** `KhatmahPlan` يوزّع المتبقي على الأيام الباقية ويقيس التقدّم من سجل القراءة نفسه |
| H02 | ✅ | الورد اليومي المركّب (قرآن + أذكار + تسبيح) | **منفَّذ:** بطاقة في الرئيسية بشريط تقدّم، وكل بند يفتح شاشته |
| H03 | ✅ | سلسلة الأيام وخريطة السنة الحرارية | **منفَّذ:** صفحة إحصاءات بسلسلة الأيام وخريطة 53 أسبوعاً وأعمدة الأسبوع، من سجل يُملأ تلقائياً أثناء القراءة |
| H04 | ✅ | وضع الحفظ: إخفاء تدريجي وكشف | **منفَّذ:** ثلاثة مستويات (مخفي/أول الحروف/ظاهر) وكشف كلمة بكلمة باللمس |
| H05 | ✅ | مراجعة بتباعد متزايد | **منفَّذ:** صناديق 1/2/4/7/15/30/60 يوماً بتقييم أعد/جيد/سهل، وقائمة «مراجعة اليوم» |
| H06 | ✅ | اختبار «أكمل الآية» | **منفَّذ:** أسئلة تُولَّد من محفوظاتك (أو قصار السور) بأربعة خيارات ونتيجة |
| H07 | ✅ | تقرير أسبوعي قابل للمشاركة | **منفَّذ:** الأسبوع من السبت إلى الجمعة، بأعمدة الأيام ومقارنة واحدة بالأسبوع الماضي، ومشاركته كصورة. لا هدف ولا لون أحمر: يقول ما حدث ويقف |
| H08 | ⏸️ | ختمة جماعية | **مؤجَّلة بطلب المستخدم:** تحتاج خلفية (Firebase/Supabase) وحسابات |

## 4) التنبيهات والإشعارات (11)

| # | الحالة | الفكرة | التنفيذ |
|---|---|---|---|
| N01 | ✅ | جدولة 7 أيام مقدماً + إعادة الجدولة بعد التشغيل | **منفَّذ:** `zonedSchedule` + `timezone` + `exactAllowWhileIdle` + boot receiver، وإعادة جدولة عند تغيّر الموقع أو الطريقة |
| N02 | ✅ | أذان كامل مع اختيار المؤذن | **منفَّذ:** ثلاثة أذانات مرفقة + استيراد أي ملف صوتي من الجهاز + اختيار من أصوات النظام + أذان مستقل للفجر |
| N03 | ✅ | وضع مستقل لكل صلاة (أذان/نغمة/اهتزاز/صامت/إيقاف) | **منفَّذ:** `PrayerAlertMode` بقناة أندرويد لكل وضع + ترحيل الإعدادات القديمة |
| N04 | ✅ | تنبيه قبل الأذان وتنبيه الإقامة | **منفَّذ:** إزاحة قابلة للضبط (0–30 د) لكليهما من مركز التنبيهات |
| N05 | ⬜ | إسكات الهاتف تلقائياً وقت الصلاة | `ACCESS_NOTIFICATION_POLICY` + استعادة مضمونة للحالة |
| N06 | ✅ | أذكار الصباح والمساء مرتبطة بالفجر والعصر | **منفَّذ:** تُشتق من مواقيت اليوم المحسوبة مع إزاحة يختارها المستخدم |
| N07 | ✅ | تنبيهات المناسبات (الكهف، الأيام البيض، عرفة، العشر) | **منفَّذ:** ثلاثة مفاتيح — الجمعة، الصيام (ليلة الاثنين والخميس والبيض)، والمناسبات الهجرية |
| N08 | ⬜ | إشعار ثابت للصلاة القادمة والعد التنازلي | `ongoing: true` بأهمية `low` |
| N09 | ✅ | آية اليوم كإشعار موسّع بأزرار | **منفَّذ:** نص الآية كاملاً + زرا «افتح» و«استمع» يفتحان المصحف على الآية نفسها |
| N10 | ✅ | تذكير الورد في الوقت المعتاد للمستخدم | **منفَّذ:** أربعة وعشرون عدّاداً للساعات على الجهاز، لا شيء غيرها ولا شيء يُرسل. يختار **أكثر** ساعة لا متوسطها — لأن من يقرأ بعد الفجر وقبل النوم متوسطه الظهر، وهي الساعة الوحيدة التي لا يقرأ فيها — ولا يحرّك شيئاً قبل خمس جلسات |
| N11 | ✅ | مركز تنبيهات واحد + ساعات هدوء + زر اختبار | **منفَّذ:** يعرض عدد المجدول والتنبيه القادم وحالة التنبيه الدقيق، بقناة لكل نوع |

## 5) الصلاة والقبلة (7)

| # | الحالة | الفكرة | التنفيذ |
|---|---|---|---|
| P01 | ✅ | حساب المواقيت محلياً | **منفَّذ:** `PrayerCalculationService` بحزمة `adhan` + التقويم الهجري، وAladhan احتياطي فقط |
| P02 | ✅ | ضبط دقيق لكل صلاة + مذهب العصر + خطوط العرض العالية | **منفَّذ:** صفحة ضبط بمعاينة مواقيت اليوم، والإعدادات تسري على الحساب وعلى إعادة الجدولة |
| P03 | ✅ | جدول شهري وإمساكية رمضان | **منفَّذ:** جدول لأي شهر بالصلوات الستّ، تنقّل بين الشهور، تمييز الجمعة واليوم، ومشاركة نصّية وصورة قابلة للطباعة. محسوب على الجهاز فيوافق الشاشة اليومية حرفاً بحرف |
| P04 | ✅ | سجل الصلوات (مسجد/جماعة/منفرد/قضاء) | **منفَّذ:** الخمس بضغطة واحدة لكل صلاة. القضاء يُحسب صلاةً لأنه صلاة، والسلسلة تتجاهل يوماً لم يُسجَّل فيه شيء بعد |
| P05 | 🟡 | قبلة بالكاميرا مع معايرة ومؤشر دقة | **جزئي:** البوصلة أُعيد بناؤها (اهتزاز متدرّج، تنعيم، مؤشر دقة ومعايرة)؛ تبقى طبقة الكاميرا |
| P06 | ✅ | وضع السفر وكشف تغيّر المدينة | **منفَّذ:** موطن يُقاس منه، مفتاح سفر بيد المستخدم لا بالمستشعر، عتبة قصر قابلة للاختيار، وبيان القصر والجمع صلاةً صلاة. الكشف يقارن بالموطن لا بآخر قراءة، ولا يسأل عن مكان سُئل عنه |
| P07 | ✅ | أقرب المساجد | **منفَّذ:** بحث Overpass بفرعَي الوسم، ترتيب بالأقرب مع سهم اتجاه، فتح في تطبيق الخرائط، وتخزين البحث. المسجد بلا اسم يبقى في القائمة، والخادم المشغول يُقال عنه مشغول لا خالياً |

## 6) الأذكار والأدعية (7)

| # | الحالة | الفكرة | التنفيذ |
|---|---|---|---|
| Z01 | ✅ | حصن المسلم كاملاً | **منفَّذ:** 136 باباً و352 ذكراً بفضلها وتخريجها، مرفقة مع التطبيق بلا أي اعتماد على الشبكة |
| Z02 | ✅ | عدّاد لكل ذكر مع تقدّم محفوظ داخل الباب | **منفَّذ:** عدّاد وتقدّم محفوظ يُصفَّر صباحاً ومساءً، وبطاقة ذكر منسّقة بالفضل والتخريج |
| Z03 | ⬜ | سبحة متطوّرة (أهداف، أزرار الصوت، اهتزاز، سجل) | `HardwareKeyboard` + `HapticFeedback` + Hive |
| Z04 | ✅ | مكتبة أدعية مبوّبة | **منفَّذ:** الأبواب الـ136 نفسها على اثني عشر رفّاً حسب الموقف — مدخل ثانٍ لا نسخة ثانية. التبويب برقم الباب لا بمطابقة كلمات، لأن الكتاب مرتّب بالموضوع أصلاً |
| Z05 | ✅ | الرقية الشرعية بتشغيل متواصل | **منفَّذ:** طابور واحد بعدد تكرارات وآية تُضاء مع تلاوتها. وكل مقطع مكتوب بجانبه: **وارد** في السنة، أم **يُقرأ لموضوعه** — والفرق حقيقي وكثيراً ما يُطمس |
| Z06 | ✅ | أسماء الله الحسنى بالمعاني والصوت | **منفَّذ:** التسعة والتسعون بمعنى مختصر وبحث يتجاهل التشكيل، وعلى الشاشة أن **العدد** متفق عليه أما **سرد الأسماء** فمن رواية الترمذي وقد اختُلف فيه |
| Z07 | ✅ | الصلاة على النبي ﷺ بعدّاد وتنبيه الجمعة | **منفَّذ:** عدّاد بأربع صيغ **واردة فقط** وبلا هدف — الصيغ المخترعة التي معها وعود مربوطة بعدد تُركت عمداً |

## 7) التجربة والنظام (10)

| # | الحالة | الفكرة | التنفيذ |
|---|---|---|---|
| S01 | ✅ | ويدجت الشاشة الرئيسية | **منفَّذ:** ويدجت الصلاة القادمة والعد التنازلي ومواقيت اليوم والتاريخ الهجري، يتحدث مع كل إعادة جدولة |
| S02 | ✅ | نسخ احتياطي واستعادة | **منفَّذ:** ملف JSON فيه المرجعيات والملاحظات وسجل القراءة والحفظ وكل الإعدادات، مع استعادة من ملف |
| S03 | ✅ | وضع رمضان موسمي | **منفَّذ:** بطاقة تظهر تلقائياً في رمضان: اليوم، عدّاد الإمساك/الإفطار، تراويح، والعشر الأواخر، مع إمساكية الشهر |
| S04 | ✅ | حاسبة الزكاة | **منفَّذ:** مال وذهب وفضة وعروض تجارة، والحساب مكشوف سطراً سطراً. الأسعار تُدخلها أنت لا يجلبها التطبيق — سعر بائت يعطي رقماً يدفع الناس على أساسه. ودون النصاب يقول **كم ينقصك**، ومن دَينه أكبر من ماله لا يخرج له رقم سالب |
| S05 | ✅ | تقويم هجري كامل وتحويل التواريخ | **منفَّذ:** شبكة الشهر بالتاريخين، المناسبات، أيام الصيام، ومواقيت أي يوم بالضغط عليه |
| S06 | ✅ | إمكانية وصول حقيقية | **منفَّذ:** كل زر أيقونة صار له اسم يقرؤه قارئ الشاشة، واختبار يمنع رجوع ذلك؛ ولا مكان في التطبيق يثبّت مقياس الخط إلا ما يُصدَّر كصورة، مع اختبار عند ٢× |
| S07 | 🟡 | أذونات في لحظتها + اختيار المدينة يدوياً | **جزئي:** اختيار المدينة يدوياً بالبحث بالاسم أو العودة للتلقائي؛ يبقى ترتيب طلب الأذونات في الفتح الأول |
| S08 | ⬜ | روابط عميقة تفتح آية أو ذكراً | تفعيل `app_router.dart` + App Links |
| S09 | ✅ | وضع توفير البيانات وإدارة التخزين | **منفَّذ:** صفحة تخزين تقول لكل شيء كم يشغل وماذا يضيع بمسحه، ووضع توفير **بإعداد لا بتخمين نوع الشبكة**: التلاوة بنصف الجودة، ووقف تحديث قائمة القرّاء في الخلفية، وسؤال قبل أي تنزيل كبير |
| S10 | ✅ | ثيمات وزخرفة إسلامية | **منفَّذ:** ثيم موسمي لرمضان والعشر والعيدين بألوان ولافتة مرسومة، نظام خطوط موحّد، وواجهة زجاجية للتنقّل والبحث |

## خارطة الطريق

### ✅ المرحلة 1 — الأساس: اجعل ما هو موجود يعمل فعلاً (مكتملة)
- **P01** حساب المواقيت محلياً بحزمة `adhan`.
- **N01** جدولة إشعارات الصلاة سبعة أيام + إعادة الجدولة بعد إعادة التشغيل.
- **N03** وضع مستقل لكل صلاة، و**N11** مركز تنبيهات واحد بزر اختبار.
- **R01** ضمّ المصحف كاملاً وفكّ ارتباط القارئ بالشبكة.

**النتيجة (تحققت):** المصحف والمواقيت يعملان بلا إنترنت، والإشعارات تُجدول فعلياً لسبعة أيام وتُعاد بعد إعادة التشغيل.

### ✅ المرحلة 2 — القراءة: سلّم القارئ مفاتيح المصحف (مكتملة)
- **R03** لوحة تحكم القراءة، **R04** أوضاع العين، **R05** التمرير التلقائي.
- **R09** أدوات الآية، **R10** المرجعيات المتعددة.
- **R06** التفسير عند كل آية، **R11** مشاركة الآية كصورة.
- **A02** التشغيل في الخلفية، **A01** التلاوة بالآية مع التظليل المتزامن.

**النتيجة (تحققت):** تحكم كامل في الخط والوضع والتمرير، أدوات لكل آية، تفسير، مرجعيات متعددة، ومشاركة كصورة، مع تلاوة بالآية تعمل في الخلفية.

> كل بنود المرحلتين الأولى والثانية مكتملة، ومعها بنود من مراحل لاحقة أُنجزت أثناء العمل:
> R12 · R13 · R14 · A03 · A04 · N02 (كوداً) · N04 · N06 · N09 · P02 · S05.

### ✅ المرحلة 3 — الالتزام: حوّل الاستخدام إلى عادة (مكتملة)
Z01 · H01 · H02 · H03 · N06 · N07 · S01

**النتيجة (تحققت):** حصن المسلم كاملاً، وخطة ختمة تحسب وردك اليومي، وبطاقة ورد في الرئيسية،
وسلسلة أيام وخريطة سنة تُملأ تلقائياً من قراءتك، وتنبيهات الجمعة والصيام والمناسبات، وويدجت على الشاشة الرئيسية.

### ✅ المرحلة 4 — التميّز (منفَّذة عدا بندين)
R02 · H04–H06 · A03–A05 · S03 · S02 · A07 · P05 · H08

**المتبقّي:** `P05` طبقة الكاميرا فوق البوصلة (تحتاج جهازاً حقيقياً للاختبار)، و`H08` الختمة الجماعية (مؤجَّلة بطلبك).

### ✅ المرحلة 5 — تصفية المتبقّي (مكتملة)
Z04 · Z05 · Z06 · Z07 · S04 · S06 · S09 · P04 · A06 · H07 · N10
ومعها **تصدير الآية كفيديو** و**المشغّل الكامل**.

**النتيجة (تحققت):** كل بند كان يمكن إنجازه بلا جهاز حقيقي ولا خادم صار منفَّذاً ومغطّى
باختبارات. وما بقي — خمسة بنود — موقوف كلٌّ منه على سبب مسمّى: جهاز للاختبار، أو حجم
بيانات، أو خادم.

### ✅ المرحلة 6 — الصلاة في مكانها (مكتملة)
`R08` تلوين التجويد · **البثوث والراديو** · `P03` الجدول الشهري · `P06` وضع السفر · `P07` أقرب المساجد.

**النتيجة (تحققت):** التجويد يُشتقّ من الرسم العثماني نفسه لا من أصل بيانات خارجي، والإذاعات
والقنوات تعمل بمشغّل داخلي، وشاشة المواقيت صار لها جدول شهري قابل للطباعة، ووضع سفر يقيس
ولا يفتي، وقائمة بأقرب المساجد من OpenStreetMap.

### ⏳ ما بعدها
الأولوية بترتيب الكلفة مقابل الأثر: `N08` الإشعار الثابت و`N05` الإسكات التلقائي
(كلاهما يحتاج جهازاً للتحقق)، ثم `R07` الترجمة والكلمة‑بكلمة، ثم `P05` طبقة الكاميرا فوق
البوصلة. و`H08` الختمة الجماعية موقوفة على قرار خادم قبل أن تكون قرار كود.

---

أسماء الحزم للاسترشاد — تحقّق من أحدث إصدار على pub.dev قبل الإضافة كما تنصّ قواعد `AGENTS.md`.

- `SEASONAL_INTRO.md` — مواصفات مقدمات المناسبات والبرومبت الجاهز لإعادة توليد الفيديو.
